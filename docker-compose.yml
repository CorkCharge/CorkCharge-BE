services:
  corkCharge:
    image: jaypark0205/corkcharge:latest
    env_file:
      - .env
    ports:
      - "8000:9000"
    environment:
      - PROD_DB_URL=${PROD_DB_URL}
      - PROD_DB_USER=${PROD_DB_USER}
      - PROD_DB_PASSWORD=${PROD_DB_PASSWORD}
      - OPEN_API_KEY=${OPEN_API_KEY}
      - OPEN_API_BASE_URL=${OPEN_API_BASE_URL}
      - TZ=Asia/Seoul
